`$fn` where {
    $fn <: `try { $body } catch($err) { $catchBody }`,
    register_diagnostic(
        span = $fn,
        message = "Avoid try/catch, use FireflyResult or FireflyAsyncResult for error handling."
    )
}